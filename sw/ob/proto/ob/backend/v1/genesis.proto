syntax = "proto3";
package ob.backend.v1;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "ob/backend/v1/params.proto";

option go_package = "ob/x/backend/types";

// GenesisState defines the backend module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
